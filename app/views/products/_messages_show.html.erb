<h3>
  <i class="fa fa-comment-o"></i>
  评论
  <small>
    （
    <%= @message_items.any? ? "#{@product.feed.count}条评论，#{@product.feed.map(&:user_id).uniq.count}人参与" : "暂无评论" %>
    ）
  </small>
</h3>
<hr>
<div id="all_messages">
  <%= render partial: 'message', collection: @message_items %>
</div>
<%= paginate @message_items %>
