      <%= paginate @message_items %>
      <h4><%= @message_items.any? ? "评论(#{@product.feed.count}条评论, #{@product.feed.map(&:user_id).uniq.count}人参与)" : "暂无评论" %></h4>
      <div id="message_items">
        <%= render partial: 'message', collection: @message_items %>
      </div>
      <%= paginate @message_items %>