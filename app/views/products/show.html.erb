<div class="row">
  <div class="col-xs-12 col-sm-12 col-md-12">
    <div id="about_product">
      <%= render 'shared/product_head' %>
      <div id="product_info">
        <div class="product_container">
          <h3 class="product_name">
            <i class="fa fa-info-circle"></i>
            <%= @product.name %>
          </h3>
          <div class="row">
            <div class="col-xs-12 col-sm-8 col-md-6">
              <div class="product_pictures">
                <%= render "carousel_slide" %>
              </div>
            </div>
            <div class="col-xs-12 col-sm-4 col-md-6">
              <div class="product_description">
                <ul class="list-unstyled">
                  <li class="border_bottom">
                    <label>价格</label>
                    <strong>¥ <%= @product.price %></strong>
                  </li>
                  <li class="border_bottom">
                    <label>创建时间</label>
                    <span><%= @product.created_at.strftime "%Y-%m-%d %H:%M:%S" %></span>
                  </li>
                  <li class="border_bottom">
                    <label>产品描述</label>
                    <span><%= @product.description %></span>
                  </li>
                  <li>
                    <%= link_to "#", class: "btn btn-primary" do %>
                      <i class="fa fa-shopping-cart"></i>
                      购买
                    <% end %>
                  </li>
                </ul>
              </div>
            </div>
          </div>
          <hr>
          <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-12">
              <%= markdown(@product.details) %>
            </div>
          </div>
        </div>
        <hr>
        <h3>
          <i class="fa fa-bullseye"></i>
          评测
        </h3>
        <hr>
        <div class="limit_evaluates">
          <%= render 'evaluates' %>
        </div>
        <div class="messages_container">
          <h3>
            <i class="fa fa-comment-o"></i>
            评论
            <small id="message_count_prompt">
              <%= render partial: "shared/message_count_prompt", locals: { feed_count: @product.feed, messages: @messages } %>
            </small>
          </h3>
          <hr>
          <div id="all_messages">
            <%= render partial: 'messages/message', collection: @messages %>
          </div>
          <%= paginate @messages %>
          <%= render 'messages/form' %>
        </div>
      </div>
    </div>
  </div>
</div>
