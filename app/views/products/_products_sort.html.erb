<div class="row">
  <div class="col-xs-12 col-md-12">
    <div id="products_sort" class="menu_list">
      <div class="navbar_menu">
        <ul class="list-inline">
          <li>
            <i class="fa fa-sitemap"></i>
            分类：
          </li>
          <li class="<%= 'active' if params[:category].blank? %>">
            <%= link_to "全部", products_path(sort: params[:sort]) %>
          </li>
          <% @product_categories.each do |product_category| %>
            <li> / </li>
            <li class="<%= 'active' if params[:category] == product_category.id.to_s %>">
              <%= link_to product_category.name, products_path(params.merge(category: product_category.id)) %>
            </li>
          <% end %>
        </ul>
        <ul class="list-inline">
          <li>
            <i class="fa fa-sort-amount-desc"></i>
            排序：
          </li>
          <li class="<%= 'active' if params[:sort].blank? %>">
            <%= link_to "默认排序", products_path(category: params[:category]) %>
          </li>
          <li> / </li>
          <li class="<%= 'active' if params[:sort] == 'popularity' %>">
            <%= link_to "人气", products_path(params.merge(sort: "popularity")) %>
          </li>
        </ul>
      </div>
      <p>根据您的需求或者喜好进行排序。</p>
    </div>
  </div>
</div>
