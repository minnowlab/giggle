<% if signed_in? %>
  <% if current_user.collecting?(@product) %>
    <%= render 'collectionships/uncollect' %>
  <% else %>
    <%= render 'collectionships/collect' %>
  <% end %>
<% else %>
      <i class="glyphicon glyphicon-heart-empty"></i><%= @product.collectionships.count %>
<% end %>