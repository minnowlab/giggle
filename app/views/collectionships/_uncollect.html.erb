<i class="glyphicon glyphicon-heart"></i>
<%= form_for(current_user.collectionships.find_by(product_id: @product.id), html: { method: :delete }) do |f| %>
  <%= f.submit "取消收藏", class: "btn btn-xs" %>
<% end %>