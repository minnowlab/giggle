<%= render "add_product_picture_modal" %>

<div class="row">
  <div class="col-md-3">
    <p class="lead">产品管理</p>
    <div class="list-group">
      <%= link_to "添加图片", "#add_product_picture_modal", class: "list-group-item",  "data-toggle" => "modal" %>
    </div>
  </div>

  <div class="col-md-9">
    <div class="row">
      <div class="col-md-12">
        <div class="thumbnail">
          <div class="caption-full">
            <h4 class="pull-right">
              RMB <%= @product.price %>
            </h4>
            <h4>
              <i class="fa fa-glass"></i>
              <%= @product.name %>
            </h4>
            <p>
              <i class="fa fa-info-circle"></i>
              <%= @product.description %>
            </p>
            <hr>
            <p>
              <%= simple_format(@product.details) %>
            </p>
          </div>    
        </div>
      </div>
    </div>

    <div class="row">
      <hr>
      <% @product.product_pictures.each do |product_picture| %>
        <div class="col-xs-12 col-sm-6 col-md-4">
          <div class="thumbnail">
            <div class="product_picture">
              <%= image_tag product_picture.picture_url(:small) %>
            </div>
            <div class="caption">
              <%= link_to_if product_picture.cover, "已设为封面", "#", class: "btn btn-info btn-xs disabled" do %>
                <%= link_to "设置为封面", setting_cover_admin_product_picture_path(product_picture), class: "btn btn-info btn-xs", method: :patch %>
              <% end %>
              <%= link_to "删除", admin_product_picture_path(product_picture), class: "btn btn-danger btn-xs", data: { confirm: "请问您确定要删除吗？" }, method: :delete %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

</div>

