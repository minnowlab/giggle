 <%= form_for(@message, url: create_message_admin_evaluate_path) do |f| %>
  <% if @message.errors.any? %>
   <div class="row">
    <div class="col-md-12">
     <div class="alert alert-danger">
      <h4><%= pluralize(@message.errors.count, "error") %> prohibited thie message from being saved:</h4>
    
     <ul>
     <% @message.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
     <% end %>
     </ul>
    </div>
   </div>
  </div>
 <% end %>

 <div class="row">
  <div class="col-md-3">
   <div class="form-group">
    <%= f.label :content, "内容" %>
    <%= f.text_area :content, size: "40x5", placeholder: "发表内容..." %>
   </div>
    <%= f.hidden_field :user_id, value: current_user.id %>
   <hr>
 </div>
</div>
  <div class="row">
    <div class="col-md-12">
      <div class="actions">
        <%= f.submit "确认提交", class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
<% end %>
